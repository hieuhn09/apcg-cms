---
name: plan:article-video-support
description: "Video support for BriefAsia articles (reusable template for 8+ tenants): new videoMedia collection, tenant-gated Articles fields, conditional hero requirement, shared upload-integrity, resolved video object on the reader API, and brief-asia-web player wiring"
date: 10-09-26
feature: article-video-support
---

# PLAN — Video Support for Articles (BriefAsia-First, Reusable Template)

**Locked SPEC (4th revision):** `process/general-plans/active/article-video-support_10-09-26/article-video-support_SPEC_10-09-26.md` — implemented as written. This plan does not renegotiate scope.

**Date**: 10-09-26
**Status**: DRAFT — VALIDATE complete, see Validate Contract below
**Complexity**: COMPLEX (see classification below)
**Context loaded:** `process/development-protocols/implementation-standards.md` and `apcg-cms/docs/` (this repo has no `process/context/all-context.md`); `brief-asia-web/process/context/all-context.md` + its `i18n`/`cms`/`tests` groups were consulted during RESEARCH for the reader-side chain.

## Complexity Classification: COMPLEX (single plan, not a phase program)

**Why COMPLEX:** touches a new Payload collection, a DB migration on two tables, a new tenant-gating primitive, a shared-code refactor of production-incident-hardened logic (`Media.ts`), two repos, and 18 acceptance criteria — well past SIMPLE's 8–15 atomic-step ceiling.

**Why NOT a phase program:** there are not 3+ independently-validatable phases with separate gates — this is one cohesive delivery (CMS collection + gating + migration, then reader rendering) with a hard sequencing dependency (CMS must ship and regenerate types before the reader can even typecheck). A single VALIDATE pass covering both repos, run once after the full checklist, is the right gate shape. Splitting it into an umbrella + phase-plan set would add coordination overhead (registries, per-phase PVL) without a matching increase in independent-progress value.

## Overview

Add optional short-video support to BriefAsia articles: a new `videoMedia` upload
collection (video/* only, no image derivatives), four new plain (non-localized)
fields on `Articles` (`video` relationship + `videoCaption` + `videoCredit` +
`videoDescription`), a conditional hero-image requirement that fires only when a
video is attached, tenant gating via a new `video` feature flag (collection-level
for `videoMedia`, field-level for the four `Articles` fields — both UI-hidden and
API-rejected), a shared upload-integrity implementation reused by `Media` and
`videoMedia`, and a fully server-resolved video object on the public article API
that `brief-asia-web` renders in the article hero slot only (never on listings).
Everything is named by function, not by publication, so a second tenant can adopt
it later via one `Tenants.features` checkbox flip plus a documented file copy.

## Goals

1. BriefAsia editors can attach a video to an article, gated on a hero image being present.
2. No other tenant can see or set video, in the admin UI or via direct API writes.
3. The article page shows the video player (poster = hero image) only when a video exists; every listing surface always shows the static hero image.
4. `Media.ts`'s upload-integrity protections are shared, not duplicated, with the new collection.
5. The feature is a reusable template: no tenant slug hardcoded anywhere, and a short adoption note exists for future adopters.

## Scope

In scope: `apcg-cms` (collection, fields, gating, migration, shared upload-integrity module, public API resolution) and `brief-asia-web` (regenerated types, hero-slot player, no listing-surface changes needed since `ArticleView` is deliberately not widened — see Design Decision D-3 below). Out of scope: everything listed in the SPEC's Out Of Scope section (Requirement B is fully removed from this work; no engine-intake changes; no transcoding/embeds/size-limit changes; no second-tenant enablement).

## Design Decisions Carried From INNOVATE (still valid — not re-litigated)

- **D1 — dedicated `videoMedia` collection**, `mimeTypes: ["video/*"]`, no `imageSizes`. Wired into `payload.config.ts`'s multi-tenant `collections` map and its `s3Storage({ collections: {...} })` map with its own `disablePayloadAccessControl: true` + `generateFileURL`.
- **D2 — tenant gating, two halves.** `videoMedia` (whole collection) uses the EXISTING `featureGatedAccess("video", tenantManagedAccess)` primitive (same pattern as `Podcasts.ts:10`) — no new primitive needed there. The four `Articles` fields need a NEW single-record primitive `tenantHasFeature(payload, tenantId, key)` because `featureGate()` (`src/access/features.ts`) is shaped for row-list filtering, not single-doc field access; it is built by composing the EXISTING `findTenantById()` (`src/lib/tenant.ts:34-46`) with the EXISTING `featureEnabled()` (`src/lib/tenant.ts:52-56`) — genuinely new glue, zero new lookup logic.
- **Not localized.** `video`, `videoCaption`, `videoCredit`, `videoDescription` are plain (non-localized) columns on `articles` + `_articles_v`, following the `exclusive` precedent (`src/migrations/20260824_000000_add_exclusive_flag.ts`), NOT the `Media.caption`/`Media.alt` localized precedent. This is the carried decision from the task brief and reflects the smaller migration footprint the SPEC's Constraints section flags.
- **Player placement.** A conditional branch in `brief-asia-web/src/components/article/article-content.tsx`'s existing hero `<figure>` (~lines 508-551, confirmed present at those lines during VALIDATE). Listings never show video BY CONSTRUCTION — see Design Decision D-3 below, which is the concrete mechanism for the "structural, not per-card guard" requirement.
- **Type sync.** Explicit, ordered, hand-executed checklist step (Section "CMS-Before-Reader Ordering" below). No sync script is built.

## Design Decisions Superseding earlier INNOVATE output (re-decided per the 4th-revision SPEC)

- **D3 (reversed).** `heroImage` requirement is a **conditional `validate` function** on the `Articles.heroImage` field (fires only when `video` is present on the same document), NOT `required: true`. See Checklist Step 6.
- **Field-level admin-UI invisibility mechanism (new PLAN decision — SPEC left this open; CONFIRMED FEASIBLE during VALIDATE).** Payload's static `admin.condition(data, siblingData, { user, operation })` is synchronous and has no live access to the currently-selected tenant's `features.video` value (the admin's selected tenant is tracked client-side via a cookie/React context from `@payloadcms/plugin-multi-tenant`, not passed into `admin.condition`). A custom Field admin Component is required to make invisibility genuinely live (not a stale/cached boolean). **VALIDATE confirmed via direct inspection of the installed package (`@payloadcms/plugin-multi-tenant@3.85.1`, the exact version pinned in `package.json`, fetched from the npm registry) that `@payloadcms/plugin-multi-tenant/client` exports `useTenantSelection(): { selectedTenantID: number | string | undefined; ...}` — this is the hook the component needs, confirmed present by name at the exact installed version.** See Checklist Step 9 for the exact design; the documented cookie-parse + REST fetch fallback remains in the checklist as a defensive path only (e.g. if the hook throws/returns `undefined` outside the expected provider context), not because the export itself is in doubt.
- **Where caption/credit/description live.** On `Articles` (mirroring `Media`'s caption/credit/alt fields conceptually, but as plain top-level fields on the article per the Not-Localized decision above), NOT on the `videoMedia` collection. `videoMedia` itself carries no bespoke fields beyond what `upload` requires — it is a pure file-storage collection, keeping the mimetype-contamination blast radius as small as possible.

## Touchpoints

### apcg-cms

| File | Change |
|---|---|
| `src/lib/upload-integrity.ts` (NEW) | Extract `tenantSlugById`, `tenantKeyPrefix`, `prefixFromSelectedTenant`, `rememberSignedFilename`, `verifyClientUpload` from `src/collections/Media.ts` verbatim (all five are already collection-agnostic — none reference `media` by name; VALIDATE re-confirmed this by reading the current file). Export all five. |
| `src/collections/Media.ts` | Remove the five extracted functions; import them from `@/lib/upload-integrity` instead. No behavior change — this is the shared-implementation refactor Criterion 15 requires. |
| `src/collections/VideoMedia.ts` (NEW) | New collection, slug `videoMedia`. `access: featureGatedAccess("video", tenantManagedAccess)`. `hooks: { beforeChange: [rememberSignedFilename], afterChange: [verifyClientUpload] }` (imported from `upload-integrity.ts`). `upload: { mimeTypes: ["video/*"] }` — no `imageSizes`. Fields: only the plugin-injected `tenant` + a `prefix` field identical in shape to `Media.ts`'s (reusing `tenantKeyPrefix`/`prefixFromSelectedTenant` hooks from the shared module). |
| `src/lib/tenant.ts` | Add `tenantHasFeature(payload, tenantId, key)` — thin wrapper: `featureEnabled(await findTenantById(payload, tenantId), key)`. Exported alongside the existing `findTenantById`/`featureEnabled`. |
| `src/lib/constants.ts` | Add `"video"` to `FEATURE_KEYS`. **VALIDATE finding (required, see Checklist Step 4a):** `FEATURE_COLLECTIONS` is typed `Record<FeatureKey, string[]>` — a non-partial Record requiring every `FeatureKey` member. Adding `"video"` to `FEATURE_KEYS` without adding a `video:` entry to `FEATURE_COLLECTIONS` makes `npm run typecheck` FAIL. Add `video: ["videoMedia"]`. |
| `src/access/collections-article-video.ts` (NEW, or inline in `Articles.ts` if under ~15 lines) | `canSetVideo` field-access function: `isSystemAdmin(req)` short-circuit, else resolve `tenantId = toId(doc?.tenant ?? data?.tenant)`, then `await tenantHasFeature(req.payload, tenantId, "video")`. Structurally mirrors `canFlagExclusive`'s shape (`isSystemAdmin` short-circuit + tenant/doc resolution, `Articles.ts:35-49`) but is feature-flag gated rather than role gated — that is intentional: Criterion 2 is about tenant-level gating, not about which role within an enabled tenant may set the field, so no role check is needed here. |
| `src/collections/Articles.ts` | In the "Media" tab fields array (currently `heroImage`, `imageLabel`, `leadImageCaption`, `imageUrl`, lines ~427-433): add `video` (upload, `relationTo: "videoMedia"`, `access: { create: canSetVideo, update: canSetVideo }`, `admin.components.Field` = the gate component from Step 9), `videoCaption` (text, same access+admin gate), `videoCredit` (text, same), `videoDescription` (text, same access+admin gate, `validate`: required whenever `video` is present — see Step 6b). Also modify `heroImage` field: add the conditional `validate` function (Step 6a). **VALIDATE note:** the "Media" tab (like every other tab in this collection — Content, Workflow, Taxonomy, Disclosure, etc.) is an UNNAMED tab (`{ label: "Media", fields: [...] }`, no `name:` key) — Payload does not nest data under unnamed tabs, so `heroImage`/`video`/`videoDescription` are true top-level document siblings, not a nested group. This is confirmed structurally: the existing `exclusive` checkbox lives in the also-unnamed "Disclosure" tab and its migration (`20260824_000000_add_exclusive_flag.ts`) adds it as a flat `articles.exclusive` column, not a nested one. This is supporting evidence (not a live-instance proof) that `siblingData` in Steps 6a/6b should see `video`'s value — see Step 6a/6b for the residual uncertainty and mandatory fallback. |
| `payload.config.ts` | Import `VideoMedia`; add `videoMedia: {}` to the `multiTenantPlugin({ collections: {...} })` map (~line 178); add `videoMedia` to the `s3Storage({ collections: {...} })` map (~line 208) with its own `disablePayloadAccessControl: true` + `generateFileURL` block mirroring `media`'s (same `r2PublicBaseUrl` base, same `clientUploads: true` inheritance from the plugin-level config — no per-collection `clientUploads` override needed). |
| `src/migrations/{timestamp}_add_video_support.ts` (NEW) | See the rewritten Checklist Step 12/13 below — this migration now also covers `tenants.features_video` (VALIDATE finding) and uses the idempotent DDL style of `20260904_000000_dtw_dashboards_port.ts`, not the older non-idempotent style of `20260824_000000_add_exclusive_flag.ts`. |
| `src/console/data/schema.ts` | **NEW touchpoint, found during VALIDATE.** This file is a hand-maintained, explicitly self-documented ("If Payload's schema changes, update the touched columns here") read-only Drizzle mirror of Payload-managed tables, used by the separate `console` admin app. Its `tenants` table definition enumerates every existing `features_*` column (`featuresArticles: boolean("features_articles")`, … `featuresCitiesMap: boolean("features_cities_map")`). Add `featuresVideo: boolean("features_video"),` in the same style. Without this, Step below (`console/data/tenants.ts`) cannot compile. |
| `src/console/data/tenants.ts` | **NEW touchpoint, found during VALIDATE.** `getSiteConfig()` hand-builds a `features: {...}` object literal typed `Record<FeatureKey, boolean>` (non-partial — every key required) by reading each Drizzle column individually (`articles: Boolean(r.featuresArticles)`, etc.). Add `video: Boolean(r.featuresVideo),` to this literal. Required for `npm run typecheck` to pass once `"video"` is added to `FeatureKey`. **No change needed** to `src/app/(console)/console/sites/[tenant]/settings/settings-form.tsx` or `actions.ts` — both iterate `FEATURE_KEYS` generically (`FEATURE_KEYS.map(...)`, `for (const k of FEATURE_KEYS)`) and pick up a working, correctly system-admin-gated "Video" toggle automatically once the two files above compile. This is worth knowing: it is a second, arguably more realistic mechanism than the Payload admin's own Tenants screen for exercising Criterion 14 (flip a second tenant's flag with zero code change) during manual verification. |
| `src/app/api/public/articles/route.ts` | `LIST_SELECT` (line ~39, currently `{ body: false }`): add `video: false, videoCaption: false, videoCredit: false, videoDescription: false`. **VALIDATE confirmed via Payload 3.85.1 core source (`utilities/stripUnselectedFields.js`, fetched from the pinned npm version)** that exclusion is applied per exact `field.name` regardless of field type (relationship/upload fields are not special-cased) — the same mechanism that already correctly excludes `body` (richText) will exclude these four fields identically. Each field must be listed individually (there is no cascade from excluding `video` to also excluding the other three). Still perform the raw-JSON manual check in Verification Evidence as the final confirmation — this is a structural guarantee criterion (Criterion 4), so belt-and-suspenders is warranted even with source-level confirmation. |
| `src/app/api/public/articles/[slug]/route.ts` | After `scopedFind` returns `doc` (which is fetched at `depth: 2`, so `doc.video` and `doc.heroImage` are already populated relation objects): if `doc.video` is a populated object, replace it with the resolved shape via a new helper `resolveArticleVideo(doc)` (see next row). If `doc.video` is null/absent, leave the field as-is (null). |
| `src/lib/article-video.ts` (NEW) | `resolveArticleVideo(doc: { video, videoCaption, videoCredit, videoDescription, heroImage })` → `{ url, mimeType, posterUrl, caption, credit, description } \| null`. `url`/`mimeType` come from the populated `video` (a `VideoMedia` doc — `url`/`mimeType` are Payload upload-field standard properties). `posterUrl` comes from the populated `heroImage`'s `sizes.hero.url ?? url` (mirrors the "hero" size precedent used elsewhere for the largest still). `caption`/`credit`/`description` map straight from the Articles fields. Returns `null` when `doc.video` is absent (non-video articles — the field is present-but-null per the accepted public-API-visibility Constraint). |

### brief-asia-web

| File | Change |
|---|---|
| `src/payload/payload-types.ts` | HAND-COPIED from apcg-cms's regenerated file (see CMS-Before-Reader Ordering below) — brings in `videoMedia`, `Article.video`/`videoCaption`/`videoCredit`/`videoDescription`, and `Tenant.features.video`. |
| `src/lib/article-video-view.ts` (NEW) | New, SEPARATE type + function — deliberately NOT part of `article-view.ts`'s `ArticleView`/`toArticleView()`. Exports `interface ArticleVideoView { url: string; mimeType: string; posterUrl: string; caption: string \| null; credit: string \| null; description: string }` and `toArticleVideoView(article: Article): ArticleVideoView \| null`, reading the already-server-resolved `video` object the public API now returns (see apcg-cms row above) directly off the raw `Article` shape — no re-derivation, no lookups, matching Criterion 16 ("frontend does no lookups or branching"). |
| `src/app/(reader)/[locale]/article/[slug]/page.tsx` | Alongside the existing `toArticleView(article, locale)` call, add `const video = toArticleVideoView(article)`. Pass `video` as a NEW, separate prop to `<ArticleContent article={view} video={video} related={...} />` — NOT merged into `view`/`ArticleView`. This is the structural mechanism: `ArticleView` (the type every listing/card component imports) never gains a video field, so no card codepath can reach it even by accident. |
| `src/components/article/article-content.tsx` | Add `video: ArticleVideoView \| null` to `ArticleContentProps` (~line 19, confirmed present at that line during VALIDATE alongside `article`/`body`/`related`/`shareUrl`/`corrections`). In the hero `<figure>` block (~lines 508-551, confirmed present at those lines): if `video` is non-null, render a new `<VideoPlayer>` component (poster=`video.posterUrl`, `preload="none"`, standard `<video controls>` — no autoplay attribute, no `<source>` prefetch beyond what `preload="none"` already prevents) instead of the `<img>`; keep the existing `<img>`/`CoverArt` branch unchanged when `video` is null. |
| `src/components/article/video-player.tsx` (NEW) | The reusable, ~30-line, branchless player component named per Requirement C's naming constraint (`video-player`, not `briefasia-video-player`). Props: exactly the six `ArticleVideoView` fields it needs (`url`, `mimeType`, `posterUrl`, `caption`, `credit`, `description`) — no tenant/site logic inside it, so it is copy-paste-safe into a second tenant repo per Criterion 16/Requirement C. |

### Not touched (blast radius exclusions — explicit)

- `src/app/api/engine/intake/route.ts` — SPEC Constraints: "this work touches NO engine-facing code." VALIDATE confirmed: no checklist step, touchpoint row, or grep target in this plan references this file.
- Every card/listing component in `brief-asia-web` (`home-hero.tsx`, `deep-dive.tsx`, `exclusive-band.tsx`, `related-row.tsx`, `account-tabs.tsx`, `asia-spotlight.tsx`, any RSS/OG/sitemap code) — none of these read `ArticleVideoView`, none are modified; they continue to consume `ArticleView` exactly as today. This is proof of the structural guarantee, not an assertion — the plan does not touch these files at all.
- Any other tenant's `Tenants.features.video` value (defaults `false` for all 12 via `featureEnabled()`'s existing "unknown key → false" fallback (`src/lib/tenant.ts:54-56`) — no seed/migration needed to set it explicitly false). VALIDATE confirmed `console/data/schema.ts`'s `articles` Drizzle mirror does not touch heroImage/video-adjacent columns, so no further other-tenant surface is affected.
- `content-engine` repo — read-only reference per task instructions; zero writes.
- `Media.ts`'s own fields, access, or upload config — only its five helper functions move to a shared module; `Media`'s collection definition (mimeTypes, imageSizes, fields) is byte-for-byte unchanged.
- Any existing article's data — no migration/backfill of content, only schema-additive columns (nullable, default null).

## Public Contracts (new/changed surfaces visible outside this feature's own files)

- **New Payload collection `videoMedia`** — a new admin-facing and REST/GraphQL-facing collection slug. Additive; no existing collection's contract changes.
- **`Articles` document shape** gains 4 new optional fields (`video`, `videoCaption`, `videoCredit`, `videoDescription`) — additive, backward compatible (existing consumers ignore unknown fields).
- **`Tenants.features` group** gains a `video: boolean` field — additive.
- **Public API response shape** (`/api/public/articles/[slug]`) — the `video` field on a returned `doc` changes shape from "raw Payload relation" (id or populated media-shaped doc) to the resolved `{ url, mimeType, posterUrl, caption, credit, description }` object (or `null`). This is a **new, additive contract**, not a breaking change to an existing field, since `video` did not exist on `Articles` before this work. The list endpoint's contract is unchanged except that `video`/`videoCaption`/`videoCredit`/`videoDescription` are now explicitly excluded (they would otherwise have appeared as raw/unresolved noise).
- **No change** to `/api/engine/intake` request or response contracts.

## Blast Radius

- **apcg-cms:** 1 new collection file, 1 new lib file (upload-integrity), 1 new lib file (article-video resolution), 1 refactored file (`Media.ts`, behavior-preserving), 1 new access helper (small, may be inlined), edits to `Articles.ts`, `payload.config.ts`, `src/lib/constants.ts`, `src/lib/tenant.ts`, `Tenants.ts` (5 files), 2 edited public API route files, 1 new migration file, 1 new admin Field Component file (Step 9), **2 additional edited files found during VALIDATE** (`src/console/data/schema.ts`, `src/console/data/tenants.ts` — required for `Record<FeatureKey, boolean>` exhaustiveness in the separate `console` app). **Total: ~16 files touched/created**, all within `src/` — no `docs/`, no `scripts/`, no `content-engine`.
- **brief-asia-web:** 1 hand-copied generated file, 1 new lib file, 1 new component file, 1 edited component file, 1 edited page file. **Total: 5 files.**
- **Risk class:** schema/migration (Postgres ALTER on `articles`, `_articles_v`, AND `tenants` — the last found during VALIDATE — additive/nullable only, reversible via `down()`), NOT auth/billing/public-API-contract-in-the-high-risk sense (SPEC explicitly rules out the intake-contract trigger for `orchestration.md` §High-Risk Execution Handoff — this work touches no engine-facing code, so the 5-artifact evidence pack does NOT apply). No secrets, no destructive writes, no deploy/proxy changes.
- **Cross-repo dependency:** brief-asia-web CANNOT typecheck the new fields until apcg-cms's `payload-types.ts` is regenerated and hand-copied — this is a hard ordering constraint, not just a nice-to-have (see next section). **Enforceability (VALIDATE finding):** this ordering is enforced mechanically, not just by documentation — brief-asia-web's `tsc --noEmit` (Criterion 11 gate) will hard-fail on `Article.video`/`ArticleVideoView` construction if Steps 19-23 are attempted before Step 18's type copy, because the pre-copy `payload-types.ts` has no `video` field on `Article` at all. An execute-agent that jumps ahead gets an immediate, unambiguous compiler error naming the exact missing property — there is no way to silently skip the ordering and still pass Criterion 11.

## CMS-Before-Reader Ordering (hard precondition)

1. Complete ALL apcg-cms Implementation Checklist steps (below) through the migration.
2. Run `npx payload generate:types` (or the repo's equivalent `package.json` script — confirm exact script name in `apcg-cms/package.json` before running; do not assume `generate:types` is the literal script name without checking). **VALIDATE confirmed the script is `payload:generate-types` (`package.json`: `"payload:generate-types": "payload generate:types"`) and the output path is `typescript.outputFile` in `payload.config.ts`, which VALIDATE confirmed resolves to `src/payload-types.ts`.**
3. Copy the regenerated `apcg-cms/src/payload-types.ts` content into `brief-asia-web/src/payload/payload-types.ts`, overwriting it wholesale (it is already a full-file mirror today, per the SPEC's Constraints).
4. Only THEN begin the brief-asia-web Implementation Checklist steps — `tsc --noEmit` in brief-asia-web will fail on `Article.video` / `ArticleVideoView` construction until this copy has happened.

## Phase Completion Rules

This plan has ONE phase (not a multi-phase program). It is considered CODE DONE when
Implementation Checklist steps 1–24 (plus the VALIDATE-added sub-steps 4a, 5a, and the
rewritten 12/13) are complete and both repos' `typecheck`/`lint` gates
(Verification Evidence table) are green. It is considered VERIFIED only after the Agent-Probe
manual checks in Verification Evidence have been walked through and confirmed by the user —
code-complete and VERIFIED are not the same status; do not mark this ✅ VERIFIED without explicit
user confirmation of the manual checks.

## Acceptance Criteria

This plan implements all 18 acceptance criteria from the locked SPEC verbatim — see
`article-video-support_SPEC_10-09-26.md` §Acceptance Criteria for the full text, and the
Verification Evidence table below for how each is proven and which strategy applies. No
acceptance criterion is added, dropped, or reworded here.

## Implementation Checklist

### apcg-cms

1. Confirm the exact `payload generate:types` script name in `apcg-cms/package.json` and the collection-creation/migration convention in `docs/04-modules-and-data-model.md` (or nearest equivalent) before writing the migration file — this repo has no `all-context.md`; `docs/` is the source of truth per the task brief. **VALIDATE pre-confirmed:** script name is `payload:generate-types`; migration convention docs are thin (`docs/04-modules-and-data-model.md` only says composite unique indexes are "added by migration" — the authoritative convention is the migrations directory itself; see Step 12's rewritten guidance).
2. Create `src/lib/upload-integrity.ts`: move `tenantSlugById`, `tenantKeyPrefix`, `prefixFromSelectedTenant`, `rememberSignedFilename`, `verifyClientUpload` out of `src/collections/Media.ts` verbatim (no logic changes — this is a pure extraction). Export all five with their existing JSDoc comments intact (the comments document the 11-08-2026 incident; do not lose them).
3. Update `src/collections/Media.ts` to import the five functions from `@/lib/upload-integrity` instead of defining them locally. Confirm `Media.ts`'s remaining content (collection config, fields) is byte-identical apart from the import swap.
4. Add `"video"` to `FEATURE_KEYS` in `src/lib/constants.ts` (after `"citiesMap"`, alphabetical/logical placement is not enforced elsewhere in the array so append at the end).
4a. **(New — VALIDATE finding, required for typecheck to pass.)** In the same file, add `video: ["videoMedia"]` to `FEATURE_COLLECTIONS` (a `Record<FeatureKey, string[]>` — every `FeatureKey` member is a required key; omitting `video` here is a compile error the moment Step 4 lands, even though `videoMedia` is not consumed through the `/api/public/[module]/route.ts` MODULES map today).
5. Add `{ name: "video", type: "checkbox", defaultValue: false }` to the `features` group's `fields` array in `src/collections/Tenants.ts` (~line 180, after `citiesMap`).
5a. **(New — VALIDATE finding, required for both DB correctness and typecheck.)** `Tenants.features` is a named Payload `group` field, which flattens to real columns on the `tenants` table (confirmed via `src/console/data/schema.ts`: `features_articles`, `features_newsletters`, … `features_cities_map` are all real columns). Adding the `video` checkbox therefore requires a NEW `tenants.features_video` column — this is folded into the rewritten migration (Step 12) as an explicit `ALTER TABLE "tenants"` statement; do not skip it because Step 5 alone "looks" like a config-only change. Additionally update `src/console/data/schema.ts` (add `featuresVideo: boolean("features_video"),` to the `tenants` table definition, matching the existing 10 `features_*` fields' style) and `src/console/data/tenants.ts` (add `video: Boolean(r.featuresVideo),` to the `features: {...}` object literal inside `getSiteConfig()`) — both are required for `npm run typecheck` to pass once `"video"` is a `FeatureKey` member, because `SiteConfig.features` and `settings-form.tsx`'s prop type are both `Record<FeatureKey, boolean>` (non-partial). No change is needed to `settings-form.tsx` or `actions.ts` themselves — both iterate `FEATURE_KEYS` generically and will render/persist a working "Video" toggle automatically.
6. Add `tenantHasFeature(payload: Payload, tenantId: number | string, key: FeatureKey): Promise<boolean>` to `src/lib/tenant.ts`, implemented as `featureEnabled(await findTenantById(payload, tenantId), key)`. Export it.
7. Create `src/collections/VideoMedia.ts`:
   - `slug: "videoMedia"`
   - `admin: { useAsTitle: "filename", group: "Editorial" }` (no `alt` field exists here, unlike Media, so use `filename` as the title — confirm Payload upload collections always expose `filename` as a usable `useAsTitle` target before finalizing; if not, omit `useAsTitle` and let Payload's default apply)
   - `access: featureGatedAccess("video", tenantManagedAccess)` (import both from their existing locations, same as `Podcasts.ts:2-3,10`)
   - `hooks: { beforeChange: [rememberSignedFilename], afterChange: [verifyClientUpload] }` imported from `@/lib/upload-integrity`
   - `upload: { mimeTypes: ["video/*"] }` — explicitly NO `imageSizes` key at all (not an empty array — omit the key)
   - `fields: [{ name: "prefix", type: "text", index: true, admin: { hidden: true, readOnly: true }, defaultValue: prefixFromSelectedTenant, hooks: { beforeValidate: [tenantKeyPrefix] } }]` — same shape as `Media.ts`'s `prefix` field, imported hooks from `upload-integrity.ts`.
8. Create `canSetVideo` (in `src/access/collections.ts` if that file already holds `canFlagExclusive`-style helpers, otherwise inline at the top of `Articles.ts` next to `canFlagExclusive`, whichever the existing `canFlagExclusive` placement convention dictates — VALIDATE confirmed `canFlagExclusive` is defined inline at the top of `Articles.ts`, lines 35-49, NOT in `src/access/collections.ts` — follow that placement, i.e. define `canSetVideo` inline in `Articles.ts` unless there is a strong reason to do otherwise): `async ({ req, doc, data }) => { if (isSystemAdmin(req)) return true; const tenantId = toId(doc?.tenant ?? data?.tenant); if (tenantId == null) return false; return tenantHasFeature(req.payload, tenantId, "video"); }`.
9. **Admin-UI-invisibility Field Component** (design confirmed feasible during VALIDATE — see below): build a small client Field Component, e.g. `src/components/admin/VideoFieldGate.tsx` (exact directory: confirm this repo's convention for custom Payload admin components — VALIDATE confirmed via grep that there is NO existing `admin.components` usage anywhere in `src/collections/*.ts` or `src/payload*.ts` today, so this is genuinely the first one in the repo; place it under `src/components/admin/` to match this repo's existing non-Payload component layout, since there is no local admin-component precedent to follow instead). Required behavior: read the currently-selected admin tenant via `useTenantSelection()` imported from `@payloadcms/plugin-multi-tenant/client` — **VALIDATE confirmed this export exists at the exact installed version (3.85.1, pinned in `package.json`) by fetching and inspecting the published package's type declarations from the npm registry; it returns `{ selectedTenantID: number | string | undefined; ... }`, which is exactly the tenant id this component needs.** Fetch that tenant's `features.video` (via the existing authenticated `/api/tenants/:id?depth=0` REST endpoint, cached per-mount), and render `null` when false, or Payload's default field renderer for the given field type when true. **Fallback (defensive only, not because the hook is unconfirmed):** if `useTenantSelection()` is ever called outside a mounted `TenantSelectionProviderClient` (unexpected, but Payload's exact provider-nesting guarantee for arbitrary custom Field components was not verifiable from package type declarations alone), parse the `payload-tenant` cookie directly (same cookie name `getTenantFromCookie` reads server-side — VALIDATE also confirmed `getTenantFromCookie` is exported from `@payloadcms/plugin-multi-tenant/utilities` at this version, matching `Media.ts`'s existing import) client-side, then perform the same REST fetch. Wire this component into `admin.components.Field` on all four Articles fields (Step 10).
10. Edit `src/collections/Articles.ts`'s "Media" tab fields array:
    - Keep `heroImage`, `imageLabel`, `leadImageCaption`, `imageUrl` unchanged in position/config except `heroImage` gains a `validate` function (Step 6a below — folded into this step).
    - Add `video`: `{ name: "video", type: "upload", relationTo: "videoMedia", access: { create: canSetVideo, update: canSetVideo }, admin: { components: { Field: "@/components/admin/VideoFieldGate#default" } } }` (exact Payload 3.x custom-component import-map string syntax — confirm the `#default` / named-export convention against this repo's `payload.config.ts` `admin.importMap` setup, if any, before finalizing).
    - Add `videoCaption`: `{ name: "videoCaption", type: "text", access: { create: canSetVideo, update: canSetVideo }, admin: { components: { Field: ... } } }`.
    - Add `videoCredit`: same shape as `videoCaption`.
    - Add `videoDescription`: same shape, PLUS the conditional-required `validate` (Step 6b).
6a. On `heroImage`, add: `validate: (value, { siblingData }) => { if (siblingData?.video && !value) return "A hero image is required whenever a video is attached."; return true; }`. **VALIDATE finding:** the "Media" tab is an unnamed tab (`{ label: "Media", fields: [...] }`, no `name:` key) — confirmed by reading `Articles.ts` directly, and cross-checked against the `exclusive` field (also in an unnamed tab, "Disclosure"), whose migration adds it as a flat `articles.exclusive` column rather than a nested one. Unnamed tabs do not nest Payload's data model, so `heroImage` and `video` are true document-level siblings — this is supporting structural evidence (not a live-instance proof) that `siblingData` should carry `video`'s value here. This remains genuinely unconfirmed without running the admin against a live DB, so the fallback below is not optional decoration — treat it as load-bearing. **Execute-agent instruction:** implement the field-level `validate` first; the FIRST manual test in Verification Evidence (save a video-bearing article with no hero image, expect a block) is the actual proof. If that test does not block the save, do not spend more than one debugging iteration on `siblingData` — switch immediately to the documented `beforeValidate` fallback: a document-level `hooks.beforeValidate` on `Articles` that checks `data.video && !data.heroImage` and throws a Payload `ValidationError`. Record which mechanism actually shipped in the phase report.
6b. On `videoDescription`, add the mirrored conditional-required `validate`: `(value, { siblingData }) => { if (siblingData?.video && !value) return "A description is required whenever a video is attached."; return true; }` (same fallback caveat and same test-first execute-agent instruction as 6a).
11. Edit `payload.config.ts`: import `VideoMedia`; add `videoMedia: {}` to the `multiTenantPlugin({ collections: {...} })` map; add `videoMedia` to the collections array passed to `buildConfig({ collections: [...] })` (confirm this array's exact location — likely near the top-level `collections:` key, separate from the plugin's per-collection map); add a `videoMedia` entry to `s3Storage({ collections: {...} })` mirroring `media`'s `disablePayloadAccessControl: true` + `generateFileURL` block, reusing the same `r2PublicBaseUrl` constant.
12. Write the migration `src/migrations/{YYYYMMDD}_{HHMMSS}_add_video_support.ts` (get the exact timestamp/filename convention by inspecting the two most recent files in `src/migrations/` immediately before writing — do not hardcode a guessed timestamp). **VALIDATE rewrote this step's guidance — read carefully, it changed the DDL style and added a table:**
    - **Style:** mirror `20260904_000000_dtw_dashboards_port.ts` (the MOST RECENT migration in this repo, which also introduces a brand-new table), NOT `20260824_000000_add_exclusive_flag.ts`. Use `IF NOT EXISTS` on every `ADD COLUMN`/`CREATE TABLE`/`CREATE INDEX`, `IF EXISTS` on every `down()` drop, and wrap FK `ADD CONSTRAINT` statements in `DO $$ BEGIN ... EXCEPTION WHEN duplicate_object THEN null; END $$;` (Postgres has no `ADD CONSTRAINT IF NOT EXISTS`). The 20260904 migration's own comment explains why: "the FK/index statements must not run before the table exists in the same batch on a re-run." This plan's migration has the exact same table-creation-plus-FK shape, so it inherits the exact same hazard.
    - **`articles` / `_articles_v` (unchanged from the original plan, just re-styled):** `ALTER TABLE "articles" ADD COLUMN IF NOT EXISTS "video_id" integer, ADD COLUMN IF NOT EXISTS "video_caption" text, ADD COLUMN IF NOT EXISTS "video_credit" text, ADD COLUMN IF NOT EXISTS "video_description" text;` plus the `_articles_v` equivalent with `version_` prefixes, FK constraints (`articles_video_id_videoMedia_id_fk` → `videoMedia.id` ON DELETE SET NULL, and the `_articles_v` equivalent) via the `DO $$ ... EXCEPTION` wrapper, and `CREATE INDEX IF NOT EXISTS` on both `video_id` columns — mirroring the exact shape of `20260702_231336_initial_schema.ts:872,1061` (hero_image FK/index).
    - **`tenants` (NEW — VALIDATE finding, was entirely missing from the original plan):** `ALTER TABLE "tenants" ADD COLUMN IF NOT EXISTS "features_video" boolean DEFAULT false;`. This is required because `Tenants.features` is a named Payload group field that flattens to real columns (Step 5a). Without this statement, adding the `video` checkbox to `Tenants.ts`'s config would make Payload expect a column that does not exist in the database — a runtime SQL error on the very next Tenants read/write after deploy, since migrations auto-apply via `scripts/migrate-prod.mjs` in `vercel-build`.
    - **`videoMedia` table:** see the rewritten Step 13 below for whether this migration file also creates the table or a companion migration does — either way, the `CREATE TABLE IF NOT EXISTS "videoMedia" (...)` statement (wherever it lives) MUST execute before the `articles`/`_articles_v` FK `ADD CONSTRAINT ... REFERENCES "videoMedia"("id")` statements run, in every code path (single file or two ordered files). Make this ordering explicit in the migration file's own comments so a future migration-fix pass does not accidentally reorder it.
    - Write a symmetric `down()` using `IF EXISTS` throughout: drop the indexes, then the FKs (or leave FK drops to cascade with column drops per the existing repo pattern — check `20260904`'s `down()` for the exact convention), then the four `articles`/`_articles_v` column pairs, then the `tenants.features_video` column.
13. Confirm (do NOT hand-write, UNLESS Step 13a's fallback applies) that Payload's own schema-push/migration tooling will create the `videoMedia` table itself (tenant + prefix columns, standard upload-collection columns) the first time migrations run against a dev DB — verify by running the migration generation command locally per this repo's documented workflow BEFORE committing to the hand-written migration file's exact column set, since Payload may bundle the new collection's own table creation into the same auto-generated migration rather than requiring a second hand-written one. Adjust Step 12 to include or exclude the `videoMedia` table DDL based on this confirmation.
13a. **(New — VALIDATE finding: concrete fallback for when no live DB is available.)** Neither repo has `node_modules` installed and this plan does not confirm a live local Postgres is provisioned for the execute-agent's environment. If `payload migrate:create`/local generation genuinely cannot be run, hand-write the `videoMedia` table directly using the idempotent style from Step 12, mirroring the `media` table's own column shape from `20260702_231336_initial_schema.ts` MINUS the `sizes_*` columns (videoMedia has no `imageSizes`) and MINUS `alt`/`caption`/`credit` (videoMedia has no bespoke fields beyond `prefix`): `id serial PRIMARY KEY, tenant_id integer, prefix text, url varchar, thumbnail_u_r_l varchar, filename varchar, mime_type varchar, filesize numeric, width numeric, height numeric, focal_x numeric, focal_y numeric, updated_at timestamp(3) with time zone DEFAULT now() NOT NULL, created_at timestamp(3) with time zone DEFAULT now() NOT NULL`, plus the `tenant_id` FK constraint (mirror `media`'s tenant FK) and a `prefix` index (mirror `media`'s `prefix` index). State explicitly in the phase report which path (generated vs. hand-written) was actually used.
14. Edit `src/app/api/public/articles/route.ts`: change `LIST_SELECT` to `{ body: false, video: false, videoCaption: false, videoCredit: false, videoDescription: false } as const`. **VALIDATE confirmed** (via Payload 3.85.1's `utilities/stripUnselectedFields.js`, read directly from the pinned npm package) that Payload's select-exclusion applies uniformly per field name regardless of type — the same mechanism already correctly excluding `body` will exclude these four fields the same way; no relationship/upload special-casing exists that would leak `video` back in. Still perform the raw-JSON manual check in Verification Evidence as final confirmation (belt-and-suspenders on a structural-guarantee criterion).
15. Create `src/lib/article-video.ts` exporting `resolveArticleVideo(doc)`: reads `doc.video` (expects a populated `VideoMedia` doc when `depth >= 1`), `doc.heroImage` (populated `Media` doc), `doc.videoCaption`, `doc.videoCredit`, `doc.videoDescription`; returns `null` if `doc.video` is not a populated object (absent/id-only/null); otherwise returns `{ url: video.url, mimeType: video.mimeType, posterUrl: heroImage?.sizes?.hero?.url ?? heroImage?.url ?? "", caption: doc.videoCaption ?? null, credit: doc.videoCredit ?? null, description: doc.videoDescription ?? "" }`.
16. Edit `src/app/api/public/articles/[slug]/route.ts`: after `const doc = result.docs[0];` and the existing not-found check, add `const resolvedVideo = resolveArticleVideo(doc); const responseDoc = { ...doc, video: resolvedVideo };` and return `jsonPublic(request, { doc: responseDoc }, 200)` instead of the raw `doc`.
17. Run `npm run typecheck` and `npm run lint` in `apcg-cms`. Fix until green.

### CMS-Before-Reader Ordering gate (see dedicated section above) — execute before continuing

18. Regenerate `payload-types.ts` in apcg-cms (`npm run payload:generate-types`, confirmed Step 1) and hand-copy into `brief-asia-web/src/payload/payload-types.ts`.

### brief-asia-web

19. Create `src/lib/article-video-view.ts`: `export interface ArticleVideoView { url: string; mimeType: string; posterUrl: string; caption: string | null; credit: string | null; description: string }` and `export function toArticleVideoView(a: Article): ArticleVideoView | null` — reads `a.video` (now typed per the copied `payload-types.ts` as the resolved shape, since the public API returns it pre-resolved — this function is a thin pass-through/type-narrowing, NOT a re-derivation, matching Criterion 16). Guard against `a.video` being `null`/absent (non-video article) by returning `null`.
20. Create `src/components/article/video-player.tsx`: a functional component taking the six `ArticleVideoView` fields as props (or the whole `ArticleVideoView` object), rendering `<video controls preload="none" poster={posterUrl} aria-label={description}>` with a single `<source src={url} type={mimeType} />` child, and rendering `caption`/`credit` in a `<figcaption>` matching the existing hero figcaption's visual style (reuse the same inline style object literal from `article-content.tsx` rather than inventing new CSS). No autoplay attribute anywhere. No `<video>` prop that would trigger eager loading (verify `preload="none"` is sufficient and no `autoPlay` default sneaks in from any shared video-element wrapper — there is none today, so this is a from-scratch element).
21. Edit `src/app/(reader)/[locale]/article/[slug]/page.tsx`: add `import { toArticleVideoView } from "@/lib/article-video-view";`, compute `const video = toArticleVideoView(article);` alongside the existing `const view = toArticleView(article, locale);` in the page's main render function (NOT in `generateMetadata`, which only needs `view` for OG tags — video does not affect metadata per Out Of Scope), and pass `video={video}` as a new prop to `<ArticleContent>`.
22. Edit `src/components/article/article-content.tsx`: add `video: ArticleVideoView | null;` to the props interface (~line 19, alongside `article: ArticleView;`); import `ArticleVideoView` type and the new `VideoPlayer` component; in the hero `<figure>` block (~lines 508-551), wrap the existing `{article.heroImageFullUrl ? <img .../> : <CoverArt .../>}` ternary with an outer check: if `video` is non-null, render `<VideoPlayer {...video} />` instead of the whole ternary; otherwise render the ternary exactly as it exists today (byte-identical for the non-video case).
23. Run `npm run typecheck` and `npm run lint` in `brief-asia-web`. Fix until green.

### Adoption note (deliverable, not an afterthought)

24. Write `process/general-plans/active/article-video-support_10-09-26/article-video-support_ADOPTION-NOTE_10-09-26.md` — one page, three sections: (a) **Files to copy** — `src/lib/article-video-view.ts`, `src/components/article/video-player.tsx`, and the video-branch edit shape for the adopting site's own hero-figure component (named per-site, so describe the edit pattern rather than a literal diff); (b) **What to wire up** — regenerate/copy `payload-types.ts` from apcg-cms first (name the exact CMS-Before-Reader Ordering dependency), then thread a `video` prop the same way Step 21/22 do, keeping it OFF the shared list/card view type; (c) **What the CMS side needs** — flip the target tenant's `Tenants.features.video` checkbox in the apcg-cms admin (or the `console` app's per-tenant settings page — VALIDATE confirmed both surfaces work and are both system-admin-gated); no code change in apcg-cms is required for a second tenant, since the field/collection is already there org-wide, gated purely by the flag.

## Data Flow (prose architecture note)

1. Editor (BriefAsia tenant, `video` flag on) uploads a file to the `video` field on an `Articles` doc in the Payload admin → browser signs a presigned PUT directly to R2 (same `clientUploads: true` path `Media` already uses, inherited at the plugin level — no per-collection override needed) → `VideoMedia`'s `beforeChange`/`afterChange` hooks (shared with `Media`) verify/relocate the object exactly as they do for images today.
2. On save, `Articles`' conditional `validate` functions (Steps 6a/6b) block the save if `video` is present without `heroImage`/`videoDescription`.
3. A reader's browser requests `/api/public/articles/[slug]` → `scopedFind` fetches the doc at `depth: 2` (already populating `heroImage` and, newly, `video`) → `resolveArticleVideo()` computes the resolved object server-side → the route returns `doc.video` as that resolved object (or `null`).
4. `brief-asia-web`'s article page calls `toArticleVideoView()` (pure pass-through/type-narrow, no network call, no lookup) and threads the result as a prop parallel to, not merged into, `ArticleView` → `article-content.tsx` renders `<VideoPlayer>` in the hero slot only if `video` is non-null.
5. Every listing/card codepath continues to call `toArticleView()` only, whose output type never includes a video field — there is no code path by which a card component could render or fetch the video, because the data simply is not there to reach.
6. Cross-tenant isolation: for any tenant with `features.video` false/absent, `canSetVideo` denies field-level writes server-side (silent field-value discard, matching `exclusive`'s established Payload field-access-denial semantics) and the admin Field Component renders nothing — both enforced independently, satisfying Criterion 2's dual (UI + API) requirement.

## Failure Modes / Edge Cases (from `vc-scenario`-style review of the 4 highest-risk checklist items)

**Risk 1 — the admin Field Component (Step 9) cannot get live tenant-feature state client-side (Payload/plugin API mismatch).**
- Edge case: `@payloadcms/plugin-multi-tenant/client` does not export a stable selection hook at v3.85.1, or exports it under a different name.
- **Resolved during VALIDATE:** confirmed present (`useTenantSelection`, exact name, exact installed version 3.85.1) by fetching and reading the published package's type declarations directly. This risk is downgraded from "genuinely uncertain" to "residual: provider-nesting for a custom Field component was not independently verifiable from type declarations alone."
- Edge case: the fallback REST fetch to `/api/tenants/:id` races the admin form's initial render, briefly showing the field before the fetch resolves.
- Mitigation: default to `null` (hidden) while loading, flip to visible only on a confirmed `true` — fail closed, not open. Document this explicitly in the component so a future editor doesn't "fix" a flash-of-hidden-content bug by defaulting to visible.

**Risk 2 — conditional `validate`'s `siblingData` may not see `video`'s value if the fields are nested under different tabs/groups (Step 6a/6b).**
- Edge case: `heroImage` and `video` are declared in the same "Media" tab fields array (flat, unnamed tab — confirmed during VALIDATE, see Step 6a), so `siblingData` at the document level should include both — but Payload's exact `siblingData` scope for `validate` on a field inside a `tabs`-type field's `fields` array was not verified against a live running instance (no DB/Payload runtime available in this environment).
- Mitigation: the checklist names an explicit fallback (document-level `beforeValidate` hook) precisely so this isn't a silent gap if the assumption is wrong; execute-agent must test this specific case manually (save video without hero → expect block) before considering Step 10/6a/6b done, not just typecheck-green. See the test-first instruction added to Step 6a.

**Risk 3 — `LIST_SELECT` exclusion of a relationship field (`video: false`) may not fully suppress it from the JSON response (Step 14).**
- Edge case: Payload's `select` option semantics for excluding upload/relationship fields sometimes behave differently from excluding plain text/richText fields (the existing `body: false` precedent is a richText field, not a relationship).
- **Resolved during VALIDATE:** confirmed via Payload 3.85.1 core source (`utilities/stripUnselectedFields.js`) that the exclusion mechanism is field-name-generic with no type-based special-casing. Residual risk is low; the manual raw-JSON check remains in Verification Evidence as final confirmation on a structural-guarantee criterion.

**Risk 4 — `tenants.features_video` migration gap (NEW — found during VALIDATE, not in the original plan).**
- Edge case: `Tenants.features` is a named Payload `group` field, so adding the `video` checkbox is a real schema change (a new `features_video` column), not a config-only change. The original plan's migration (Step 12) only touched `articles`/`_articles_v` and never mentioned `tenants` at all.
- Mitigation: Step 12 now includes an explicit `ALTER TABLE "tenants" ADD COLUMN IF NOT EXISTS "features_video" boolean DEFAULT false;` statement, and Step 5a adds the required companion updates to the hand-maintained Drizzle mirror (`src/console/data/schema.ts`) and the `console` app's `getSiteConfig()` (`src/console/data/tenants.ts`) so `Record<FeatureKey, boolean>` stays satisfied everywhere it is used.

## Dependencies

- `apcg-cms`'s exact `generate:types` script name (confirmed: `payload:generate-types`) and migration filename convention (Checklist Step 1) — confirm the timestamp by inspecting the two most recent files in `src/migrations/` immediately before writing (Step 12).
- `@payloadcms/plugin-multi-tenant/client`'s `useTenantSelection` export (Checklist Step 9) — confirmed present at the installed version (3.85.1) during VALIDATE; no longer a blocking unknown.
- Branch: `claude/tender-ptolemy-njgwwt` in both repos (per SPEC Constraints) — confirm both repos are on this branch before EXECUTE begins.

## Risks (carried to Validate Contract)

| Risk | Class | Mitigation |
|---|---|---|
| `videoMedia` table auto-creation vs hand-written migration overlap (Checklist Step 13) | Migration correctness | Confirm via local migration-generation run before finalizing Step 12's file content; concrete hand-written fallback DDL provided in Step 13a if no live DB is available |
| `tenants.features_video` column entirely missing from migration (found during VALIDATE) | Migration correctness — production-breaking if missed | Explicit `ALTER TABLE "tenants"` statement added to Step 12; companion Drizzle-mirror + console-app updates added to Step 5a |
| Migration DDL style regression (older non-idempotent style vs. newer idempotent style, found during VALIDATE) | Migration correctness on deploy/re-run | Step 12 now mirrors `20260904_000000_dtw_dashboards_port.ts`'s `IF NOT EXISTS`/`DO $$ EXCEPTION` style, not `20260824`'s |
| Admin Field Component library-API mismatch (Risk 1 above) | Implementation blocker | Resolved during VALIDATE — hook confirmed present at installed version; fallback retained as defensive-only |
| Conditional `validate` siblingData scope (Risk 2 above) | Silent acceptance-criterion failure | Documented fallback + mandatory test-first manual check before Step 10 is considered done |
| `LIST_SELECT` relationship-field exclusion semantics (Risk 3 above) | Structural guarantee not actually enforced | Resolved during VALIDATE via Payload core source read; manual raw-JSON check remains as final confirmation |
| Cross-repo type-sync step silently skipped | Reader typecheck failure disguised as "unrelated" error | CMS-Before-Reader Ordering is a named, gated checklist section; brief-asia-web's own `tsc --noEmit` hard-fails if skipped (mechanically enforced, confirmed during VALIDATE) |

## Verification Evidence

_Test Procedure note: both repos have no automated test runner beyond `tsc --noEmit` + lint (confirmed via `brief-asia-web/process/context/tests/all-tests.md` and `apcg-cms/docs/13-acceptance-criteria.md`, both re-confirmed during VALIDATE); every other row below is Agent-Probe manual verification, consistent with that constraint. No test gate below is invented beyond what these two commands (per repo) actually provide — every other row is honestly labeled Agent-Probe per the SPEC's own strategy labels._


| Gate / Scenario | Strategy | Proves SPEC criterion |
|---|---|---|
| `cd apcg-cms && npm run typecheck` (`tsc --noEmit`) | Fully-Automated | Criterion 9 (additive/nullable schema, no break), Criterion 11 (apcg-cms typecheck) — also the gate that catches Checklist Steps 4a/5a if they are ever missed |
| `cd apcg-cms && npm run lint` (`next lint`) | Fully-Automated | Criterion 11 |
| `cd brief-asia-web && npm run typecheck` | Fully-Automated | Criterion 11 (brief-asia-web typecheck) |
| `cd brief-asia-web && npm run lint` | Fully-Automated | Criterion 11 |
| `grep -rEn "brief-asia\|briefasia\|dtw-web\|wad-web\|gcv-web\|wtb-web\|asia-awards-web\|dailytechwire-web\|APCG-web" src/collections/VideoMedia.ts src/lib/upload-integrity.ts src/lib/tenant.ts src/lib/article-video.ts src/access/collections.ts src/collections/Articles.ts src/console/data/schema.ts src/console/data/tenants.ts (video-related hunks only) — repeat for brief-asia-web's src/lib/article-video-view.ts and src/components/article/video-player.tsx` | Fully-Automated | Criterion 13 (no tenant slug hardcoded) — zero matches = pass, other than doc comments explicitly describing BriefAsia as reference implementation. **VALIDATE note:** the grep command now includes the two `console/data/*` files added by Step 5a — the original command would have missed a slug hardcoded there. |
| `git diff payload.config.ts \| grep "fileSize"` (expect no output) | Fully-Automated | Criterion 10 (20MB ceiling unchanged) |
| Grep confirming `Media.ts` and `VideoMedia.ts` both import `rememberSignedFilename`/`verifyClientUpload` from `@/lib/upload-integrity` and neither file defines them locally | Fully-Automated | Criterion 15 (shared, not duplicated, upload-integrity) |
| Grep confirming collection slug `videoMedia`, component name `video-player`, `FEATURE_KEYS` entry `video` | Fully-Automated | Criterion 18 (function-based naming) |
| File-existence + section-presence check on `article-video-support_ADOPTION-NOTE_10-09-26.md` (3 named sections present) | Fully-Automated | Criterion 17 (adoption note exists) |
| Manual: BriefAsia editor uploads a video (<20MB) with hero image present, saves, reloads, confirms persistence | Agent-Probe | Criterion 1 |
| Manual: attempt to upload a video >20MB, confirm same rejection UX as an oversized image | Agent-Probe | Criterion 10 |
| Manual: open article editor for a non-BriefAsia tenant, confirm `video`/`videoCaption`/`videoCredit`/`videoDescription` fields are absent from the DOM (inspect element, not just visually hidden) | Agent-Probe | Criterion 2 (UI half) |
| Manual/local-API: attempt a direct Payload local-API `update` call on a non-enabled tenant's article setting `video`, confirm the field value is discarded/rejected, never persisted | Agent-Probe | Criterion 2 (API half) |
| Manual: BriefAsia article WITH video — article page shows video player in hero slot, poster = hero image | Agent-Probe | Criteria 3, 6 |
| Manual: BriefAsia article WITHOUT video — article page hero slot pixel/behavior-identical to pre-feature production | Agent-Probe | Criterion 3 |
| Manual + raw JSON inspection: homepage, pillar front, related-articles, tag page, author page, search results, RSS, OG/social-share image for a video-bearing article — confirm `<img>` only, never `<video>`, AND confirm raw `/api/public/articles` (list) JSON has no `video`/`videoCaption`/`videoCredit`/`videoDescription` keys at all | Agent-Probe | Criterion 4 (both DOM AND network-tab/JSON layers, per Risk 3 — mechanism now source-confirmed, this check is the final proof) |
| Manual, browser network tab: on a video article page, confirm no video byte-range request occurs before the reader presses play; confirm full controls present after | Agent-Probe | Criterion 5 |
| Manual: save video with hero image present (poster reuse) + separately attempt save with video but no hero (expect validation block — this is the load-bearing test for Risk 2/Step 6a, run it FIRST before considering Step 10 done) + save non-video article with no hero (expect success, unchanged) | Agent-Probe | Criteria 6, 12 |
| Manual: save video with caption/credit filled, and separately with both blank; both succeed | Agent-Probe | Criterion 7 |
| Manual: attempt save with video but empty `videoDescription` (expect block); fill it (expect success) | Agent-Probe | Criterion 8 |
| Before/after spot-check: count of existing articles + null video-field state unchanged post-deploy for a BriefAsia + a non-BriefAsia sample | Agent-Probe (+ Fully-Automated typecheck as the schema-additivity proof) | Criterion 9 |
| Manual: flip a second (non-BriefAsia) tenant's `video` flag ON in a local/staging environment with zero source edits — via EITHER the Payload admin's Tenants screen OR the `console` app's per-tenant settings page (both confirmed working, both system-admin-gated, during VALIDATE) — confirm field appears in that tenant's editor and resolved video object appears in that tenant's article API response | Agent-Probe | Criterion 14 |
| Manual/API inspection: fetch a video-bearing article via `/api/public/articles/[slug]`, confirm all six `url`/`mimeType`/`posterUrl`/`caption`/`credit`/`description` fields present and fully resolved (posterUrl is a real usable URL) | Agent-Probe | Criterion 16 |
| Manual: load the Payload admin's Tenants list/detail view and the `console` app's per-tenant settings page for ANY tenant after the migration lands, confirm no SQL/500 error (proves the `tenants.features_video` migration actually applied) | Agent-Probe | Regression check for the VALIDATE-found Risk 4 (not a numbered SPEC criterion, but load-bearing for Criterion 11's "both repos' gates stay green" in the broader sense of "the app doesn't 500") |

## Test Infra Improvement Notes

(none identified yet — both repos' lack of an automated test runner is a pre-existing, out-of-scope condition, not something this feature should attempt to fix; see Verification Evidence note above)

## Resume and Execution Handoff

1. **Selected plan file path:** `process/general-plans/active/article-video-support_10-09-26/article-video-support_PLAN_10-09-26.md` (this file).
2. **Last completed phase or step:** VALIDATE — validate-contract written below, plan updated in place with VALIDATE findings (Steps 4a, 5a, 13a; rewritten Steps 9, 12, 14; new touchpoint rows for `src/console/data/*`).
3. **Validate-contract status:** written below — see Gate line for verdict.
4. **Supporting context files loaded:** locked SPEC (`article-video-support_SPEC_10-09-26.md`), `apcg-cms/src/collections/{Media,Articles,Tenants,Podcasts}.ts`, `apcg-cms/src/access/{features,collections,helpers}.ts`, `apcg-cms/src/lib/{tenant,constants}.ts`, `apcg-cms/src/console/data/{schema,tenants}.ts`, `apcg-cms/payload.config.ts`, `apcg-cms/src/migrations/{20260702_231336_initial_schema,20260824_000000_add_exclusive_flag,20260904_000000_dtw_dashboards_port}.ts`, `apcg-cms/src/app/api/public/{[module],articles/route,articles/[slug]/route}.ts`, `apcg-cms/package.json`, `@payloadcms/plugin-multi-tenant@3.85.1` and `payload@3.85.1` package sources (fetched from the npm registry during VALIDATE, since neither repo has `node_modules` installed), `brief-asia-web/src/lib/article-view.ts`, `brief-asia-web/src/components/article/article-content.tsx`, `brief-asia-web/src/app/(reader)/[locale]/article/[slug]/page.tsx`, `brief-asia-web/src/payload/payload-types.ts` (partial), `brief-asia-web/process/context/tests/all-tests.md`.
5. **Next step for a fresh agent picking up mid-execution:** VALIDATE has run — see the Gate line below for PASS/CONDITIONAL/BLOCKED. If CONDITIONAL, the orchestrator runs a plan-validate-fix supplement cycle before EXECUTE is legal (see Open Gaps in the Validate Contract). If PASS or an accepted CONDITIONAL, say `ENTER EXECUTE MODE` next and re-read this plan's Implementation Checklist numbering (1–24 plus 4a/5a/6a/6b/13a) and the CMS-Before-Reader Ordering gate to determine exact resume point — apcg-cms steps (1–18, including 4a/5a/13a) must ALL be complete, including the type-copy at Step 18, before any brief-asia-web step (19–23) is attempted.

## Validate Contract

Status: CONDITIONAL
Date: 10-09-26
date: 2026-09-10
generated-by: outer-pvl

Parallel strategy: parallel-subagents
Rationale: 4 Layer 1 dimension checks (infra/test-coverage/breaking-changes/security) plus 5 independent Layer 2 section checks (upload-integrity extraction, conditional-validate/siblingData, admin field-visibility component, LIST_SELECT exclusion, migration) have no cross-agent dependencies during investigation and were synthesized once at the end — no coordination needed mid-run, so parallel fire-and-forget subagents are the right shape rather than an agent team.

Test gates (C3 5-column table):

| criterion id | behavior | strategy | proving test | gap-resolution |
|---|---|---|---|---|
| 9, 11 | apcg-cms schema stays additive/nullable; both repos typecheck clean | Fully-Automated | `cd apcg-cms && npm run typecheck` | A |
| 11 | apcg-cms lint clean | Fully-Automated | `cd apcg-cms && npm run lint` | A |
| 11 | brief-asia-web typecheck clean (also proves CMS-Before-Reader ordering was honored — fails hard otherwise) | Fully-Automated | `cd brief-asia-web && npm run typecheck` | A |
| 11 | brief-asia-web lint clean | Fully-Automated | `cd brief-asia-web && npm run lint` | A |
| 13 | no tenant slug hardcoded in any touched file, including the two console/data files found during VALIDATE | Fully-Automated | `grep -rEn "brief-asia\|briefasia\|dtw-web\|wad-web\|gcv-web\|wtb-web\|asia-awards-web\|dailytechwire-web\|APCG-web" <touched files incl. src/console/data/schema.ts src/console/data/tenants.ts>` | A |
| 10 | 20MB upload ceiling unchanged | Fully-Automated | `git diff payload.config.ts \| grep "fileSize"` (expect no output) | A |
| 15 | upload-integrity shared, not duplicated | Fully-Automated | grep confirming both `Media.ts` and `VideoMedia.ts` import from `@/lib/upload-integrity`, neither defines locally | A |
| 18 | function-based naming (`videoMedia`, `video-player`, `video` feature key) | Fully-Automated | grep confirming the three names | A |
| 17 | adoption note exists with 3 named sections | Fully-Automated | file-existence + section-presence check on `article-video-support_ADOPTION-NOTE_10-09-26.md` | A (once Step 24 is executed) |
| 1, 2, 3, 4, 5, 6, 7, 8, 12, 14, 16 | editor upload/save flow, cross-tenant UI+API isolation, hero-slot conditional rendering, listing-surface never-video guarantee, no-autoplay/no-prefetch, conditional hero/description validation, second-tenant zero-code-change adoption, fully-resolved reader API object | Agent-Probe | manual admin/browser/API walkthroughs listed in Verification Evidence table above (no automated harness exists in either repo) | A (executed manually at EXECUTE/EVL time) |
| Risk 4 (Tenants migration regression, not a numbered SPEC criterion) | `tenants` table loads without a SQL error after migration | Agent-Probe | load Payload admin Tenants list + console per-tenant settings page post-migration | A |

Legacy line form (retained so existing validate-contract consumers still parse):
- apcg-cms build: `npm run typecheck` | `npm run lint` — Fully-Automated
- brief-asia-web build: `npm run typecheck` | `npm run lint` — Fully-Automated
- tenant-slug hardcoding: `grep -rEn "brief-asia|briefasia|..." <touched files>` — Fully-Automated
- upload size ceiling: `git diff payload.config.ts | grep fileSize` — Fully-Automated
- shared upload-integrity: grep for single-source import — Fully-Automated
- naming convention: grep for `videoMedia`/`video-player`/`video` — Fully-Automated
- adoption note: file + section existence check — Fully-Automated
- all editor/reader/cross-tenant behavior (Criteria 1-8, 12, 14, 16): Agent-Probe — [hybrid: none available; agent-probe: manual admin/browser/API walkthrough, no e2e harness in either repo]
- Known-gap: none accepted as a silent residual — every developed behavior in this plan has either a Fully-Automated or an Agent-Probe gate naming it explicitly; see "What this coverage does NOT prove" below for what Agent-Probe genuinely cannot catch.

C-4 reconciliation: the `strategy:` column above carries only the 3 proving strategies (Fully-Automated / Hybrid / Agent-Probe). No row uses Known-Gap as a strategy.

Dimension findings:
- Infra fit: CONCERN — R2/clientUploads/upload-integrity reuse pattern is sound and precedented (`Podcasts.ts`, `Media.ts`). The admin Field Component is the first of its kind in this repo (confirmed via grep: no existing `admin.components` usage), but its core dependency (`useTenantSelection`) was confirmed present at the exact installed `@payloadcms/plugin-multi-tenant` version (3.85.1) by fetching and reading the published package's type declarations — this substantially de-risks what the plan itself flagged as its most uncertain area. Residual: whether the hook is guaranteed reachable from an arbitrary custom Field component's provider context was not independently verifiable without a live admin session.
- Test coverage: CONCERN — both repos genuinely have no automated test runner beyond `tsc --noEmit`/`next lint` (independently re-confirmed by reading `brief-asia-web/process/context/tests/all-tests.md` and `apcg-cms/docs/13-acceptance-criteria.md`); the plan does not invent gates that don't exist and honestly labels the rest Agent-Probe per the SPEC's own strategy vocabulary. This is a structural property of the repos, not a plan defect — CONCERN reflects "manual verification carries almost the entire behavioral proof burden," which is disclosed, not hidden.
- Breaking changes: CONCERN — two concrete, previously-undocumented gaps were found and fixed directly in the plan during this VALIDATE pass: (1) `FEATURE_COLLECTIONS: Record<FeatureKey, string[]>` requires a `video` entry the moment `"video"` is added to `FEATURE_KEYS`, or `npm run typecheck` fails (now Checklist Step 4a); (2) `Tenants.features` is a named Payload group field that flattens to real DB columns, and the plan's original migration never touched `tenants` at all — a genuine, production-reaching gap (now fixed in the rewritten Step 12 + Step 5a, including the hand-maintained Drizzle mirror at `src/console/data/schema.ts` and the `console` app's `getSiteConfig()`, both of which VALIDATE discovered read/write the SAME `tenants.features` columns via a parallel, hand-synced schema). Both are now closed in the plan text with a Fully-Automated gate (typecheck) confirming (1) and an explicit migration statement fixing (2) — carried as CONDITIONAL rather than PASS because (2) cannot be proven closed until the migration actually runs against a database, which no environment available to VALIDATE could do.
- Security surface: PASS — `canSetVideo` correctly denies non-enabled tenants at the API layer (field-level `access.create/update`), independent of and in addition to the admin-UI-invisibility mechanism, satisfying Criterion 2's dual requirement. `videoMedia`'s `mimeTypes: ["video/*"]` scoping avoids the cross-collection mimetype-contamination risk the SPEC flags (confirmed: no existing upload field's `relationTo` is widened). No secrets, auth, or billing surface touched. `Tenants.features` write access remains system-admin-only in both the Payload admin and the `console` app (confirmed by reading both access-control paths).
- Section — Upload-integrity extraction (Steps 2-3, 7): PASS — mechanical feasibility HIGH (all five extracted functions confirmed collection-agnostic by direct reading; none reference `media` by name). No gaps found. `verifyClientUpload`'s in-transaction `APIError` throw (rollback-on-failure) is unaffected by a pure module-move — the hook fires inside whichever collection's create transaction registers it, regardless of which file defines the function body.
- Section — Conditional validate / siblingData (Steps 6a/6b): CONCERN — mechanical feasibility MEDIUM-HIGH, supported by structural evidence (unnamed-tab flattening, confirmed via the `exclusive` field precedent) but not by a live-instance test (none available). The plan's own fallback and a new test-first execute-agent instruction are load-bearing, not optional. Highest-risk edit: the field-level `validate` call itself — mitigation is "test first, fall back fast," now made explicit.
- Section — Admin field-visibility component (Step 9): CONCERN, downgraded from the plan's own "genuinely uncertain" framing — mechanical feasibility now HIGH for the export's existence (source-verified at the pinned version), MEDIUM for provider-context reachability (architecturally plausible, not proven). No gaps beyond what the plan already names; conflicts: none found.
- Section — LIST_SELECT exclusion (Step 14): PASS, downgraded from CONCERN via source verification — Payload 3.85.1's `stripUnselectedFields` applies uniformly per field name; no relationship/upload special-casing exists. Manual raw-JSON check remains as final confirmation on a structural-guarantee criterion, not because the mechanism is in doubt.
- Section — Migration (Steps 12-13): FAIL → fixed in plan during this VALIDATE pass (gap-resolution B) — see Breaking changes above. Two concrete plan-text edits applied: (1) the `tenants.features_video` column statement added to Step 12, with companion Drizzle-mirror/console-app updates in new Step 5a; (2) the migration DDL style changed from the older non-idempotent `20260824` precedent to the newer idempotent `20260904` precedent (`IF NOT EXISTS`/`DO $$ EXCEPTION` guards), matching the most recent and safest convention actually in use in this repo. Residual CONCERN: whether Payload auto-generates the `videoMedia` table or it must be hand-written (Step 13/13a) cannot be settled without a live DB; a concrete hand-written fallback DDL is now provided (Step 13a) so execute-agent is never blocked on this.

Open gaps:
- Whether `siblingData` reliably carries `video`'s value across the unnamed "Media" tab for the conditional `validate` functions (Steps 6a/6b) — structurally likely, not proven; the plan's fallback + test-first instruction covers this. Accepted as a known, disclosed uncertainty carried into EXECUTE, not a blocking gap — the plan cannot deadlock on it (fallback always available).
- Whether Payload auto-generates the `videoMedia` table alongside the hand-written `articles`/`_articles_v`/`tenants` migration, or whether it must be hand-written in the same migration (Step 13/13a) — cannot be settled without a live DB in this environment; a concrete fallback DDL is now specified so this cannot block EXECUTE indefinitely.
- Whether a custom Payload admin Field Component is guaranteed to render inside `TenantSelectionProviderClient`'s React context (Step 9) — architecturally plausible from the plugin's design intent, not verified live; the cookie-fallback path covers this defensively.

What this coverage does NOT prove:
- `npm run typecheck`/`npm run lint` (both repos) prove type and lint soundness only — they do NOT prove the migration actually applies cleanly against a real Postgres database, that R2 client uploads for video files behave identically to image uploads under real network conditions, that the admin Field Component actually mounts and reads live tenant state in a running admin session, or that the conditional `validate` functions actually fire correctly at runtime (typecheck cannot execute a `validate` callback's logic against real `siblingData`).
- The tenant-slug grep proves the absence of a specific string pattern; it does not prove the ABSENCE of any other tenant-identifying logic (e.g. an id-based conditional that happens not to spell out a slug).
- The `fileSize`-diff grep proves the config line is textually unchanged; it does not re-verify the 20MB ceiling is still enforced at runtime (that is the separate Agent-Probe oversized-upload check).
- Agent-Probe manual walkthroughs prove the specific scenarios exercised in that walkthrough on that day, in that environment; they do not constitute regression coverage for future changes — a future refactor of `Articles.ts`, `article-content.tsx`, or the public API routes could silently reintroduce a video leak on a listing surface with no automated gate to catch it. This is an accepted, disclosed residual given neither repo has a test runner (see Test coverage dimension finding above) — not something this plan is expected to fix.
- Nothing in this VALIDATE pass executed code, ran a migration, or exercised a live Payload admin session — every finding above is derived from static source reading (including the two library packages fetched from the npm registry at their pinned versions) and structural cross-referencing (e.g. the `exclusive` field precedent), not from running the feature.

Gate: CONDITIONAL (0 unresolved FAILs after in-plan fixes; 3 disclosed, non-blocking open gaps carried forward; both migration FAILs found during this VALIDATE pass were fixed directly in the plan text above — see Breaking changes and Migration dimension findings)
Accepted by: session (VALIDATE pass, single-shot analytical review — no live user gate available in this invocation). Concerns accepted: (1) siblingData scope for Steps 6a/6b unproven without a live instance — fallback mandated; (2) videoMedia table auto-generation vs. hand-write undetermined without a live DB — concrete fallback DDL provided; (3) admin Field Component provider-context reachability architecturally inferred, not live-verified — cookie fallback mandated. None of these three block EXECUTE from proceeding; each has an explicit, checklist-level fallback that keeps the work moving if the optimistic path fails.

## Autonomous Goal Block

```
SESSION GOAL: Ship optional short-video support for BriefAsia articles (apcg-cms + brief-asia-web), built as a reusable, config-toggle-only template for 8+ other tenant repos.
Charter + umbrella plan: N/A — single COMPLEX plan, not a phase program.
Autonomy: Standard RIPER-5 autonomy. VALIDATE never skipped; a placeholder validate-contract = blocked. EXECUTE requires explicit "ENTER EXECUTE MODE" (this is CONDITIONAL, not PASS — see below).
Hard stop conditions / safety constraints:
- No engine-facing code (`src/app/api/engine/intake/route.ts`) may be touched — SPEC Constraint, verified untouched by this plan.
- No tenant slug may be hardcoded in any touched file — Criterion 13, Fully-Automated grep gate.
- Migrations are additive/nullable only, on `articles`, `_articles_v`, and `tenants` — no destructive writes, no backfill of existing content.
- 20MB upload ceiling must remain unchanged (`payload.config.ts` `upload.limits.fileSize`) — Criterion 10.
- CMS-Before-Reader Ordering is a hard sequencing gate: brief-asia-web steps (19-23) must not start before apcg-cms's `payload-types.ts` is regenerated and copied (Step 18) — mechanically enforced by brief-asia-web's own typecheck failing otherwise.
Next phase: EXECUTE — plan path: process/general-plans/active/article-video-support_10-09-26/article-video-support_PLAN_10-09-26.md
Validate contract: inline in plan (see "## Validate Contract" section above) — Gate: CONDITIONAL, 3 disclosed non-blocking open gaps, each with a mandated fallback.
Execute start: Fully-Automated gates: `cd apcg-cms && npm run typecheck && npm run lint`; `cd brief-asia-web && npm run typecheck && npm run lint`. Agent-Probe scenarios: Verification Evidence table in this plan (no e2e harness in either repo). High-risk evidence pack: no (SPEC explicitly rules out the high-risk trigger — no engine-facing/auth/billing surface touched).
```
